class Manager {

    constructor(userId, username, nom, prenom, dateNaiss, sexe, adresseResidence, affectation) {
        this.userId = userId
        this.username = username
        this.nom = nom
        this.prenom = prenom
        this.dateNaiss = dateNaiss
        this.sexe = sexe
        this.adresseResidence = adresseResidence
        this.affectation = affectation
    }

    // disconnect()
//     abstract void insererFichier(Fichier f)
//     void generateReport(String url)
//     void exportReport(Rapport r)
//     void consultPlanning(PlanningMaintenance p)
}